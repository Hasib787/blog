import React from 'react';

const NotFound = () => {
    return (
        <div>
            <h1 style={{color:'red', textAlign:'center'}}>Page is not found</h1>
        </div>
    );
};

export default NotFound;